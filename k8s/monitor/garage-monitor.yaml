apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: garage-monitor
  labels:
    name: garage-monitor
    release: prometheus
  namespace: monitor
spec:
  namespaceSelector:
    matchNames:
      - garage
  selector:
    matchLabels:
      name: garage
  serviceMetricsEndpoints:
    - port: admin  # The metrics API port name of Mount Pod
      path: '/metrics'
      scheme: 'http'
      interval: '30s'