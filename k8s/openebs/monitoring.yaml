apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mayastor-monitoring
  namespace: openebs
  labels:
    app: mayastor
    release: prometheus
spec:
  selector:
    matchLabels:
      app: mayastor
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics