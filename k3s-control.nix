{...} : {
  # services.k3s = {
  #   enable = true;
  #   role = "server";
  #   token = "my-secret-token";  # Shared with workers
  #   extraFlags = "--node-ip=$(tailscale ip -4)"; # WireGuard IP
  # };
}
